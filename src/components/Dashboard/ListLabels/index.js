import React from 'react';
import ListLabels from './styles';

const ContainerLabels = ({ children }) => {
  return (
    <ListLabels>
      {children}
    </ListLabels>
  );
};

export default ContainerLabels;
